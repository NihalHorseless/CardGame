package com.example.cardgame.data.enum

enum class FortificationType {
    WALL,   // Defensive structure with high health but no attack
    TOWER   // Structure with both attack and defense capabilities
}